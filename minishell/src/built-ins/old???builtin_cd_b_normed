/* ************************************************************************** */
/*                                                                            */
/*                                                        :::      ::::::::   */
/*   builtin_cd_b_normed                                :+:      :+:    :+:   */
/*                                                    +:+ +:+         +:+     */
/*   By: alsima <alsima@student.42.fr>              +#+  +:+       +#+        */
/*                                                +#+#+#+#+#+   +#+           */
/*   Created: 2025/09/02 22:40:42 by alsima            #+#    #+#             */
/*   Updated: 2025/09/02 22:41:00 by alsima           ###   ########.fr       */
/*                                                                            */
/* ************************************************************************** */

#include "../../inc/minishell.h"


static DIR	*handle_tilde(char ***av, char *home)
{
	DIR	*dir = NULL;

	if (av[0][1] && av[0][1][0] == '~' && ft_strlen(av[0][1]) == 1 && home && *home)
	{
		free(av[0][1]);
		av[0][1] = ft_strdup(home);
		dir = opendir(av[0][1]);
	}
	else if (av[0][1])
		dir = opendir(av[0][1]);
	return (dir);
}

static void	handle_chdir_logic(char ***av, char *home, char *oldpwd, t_cmd_set *p, DIR *dir)
{
	if ((!home || !home[0]) && ((!av[0][1] || av[0][1][0] == '~') && !dir))
		put_err_cd("HOME_Not_Set", NULL, 1, p);
	else if ((!oldpwd || !*oldpwd) && av[0][1] && av[0][1][0] == '-')
		put_err_cd("OLDPWD_Not_Set", NULL, 1, p);
	else if (!av[0][1] || av[0][1][0] == '~')
	{
		p->status_code = (chdir(home) == -1);
		if (p->status_code)
			put_err_cd("NoFile_NoDir", home, 1, p);
	}
	else if (av[0][1] && av[0][1][0] == '-' && oldpwd && *oldpwd)
	{
		p->status_code = (chdir(oldpwd) == -1);
		if (p->status_code)
			put_err_cd("NoFile_NoDir", oldpwd, 1, p);
	}
	else if (!dir)
		cd_path_checks(av[0][1], p);
	else
		chdir(av[0][1]);
}

static void	ft_chdir(char ***av, char *home, char *oldpwd, t_cmd_set *p)
{
	DIR	*dir;

	dir = handle_tilde(av, home);
	handle_chdir_logic(av, home, oldpwd, p, dir);
	if (dir)
		closedir(dir);
}